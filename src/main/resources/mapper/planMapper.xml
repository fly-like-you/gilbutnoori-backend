<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.gilbut.domain.plan.mapper.PlanMapper">

    <insert id="insertPlan" parameterType="PlanCreateRequestDTO" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO plans (
            attraction_id,
            travel_id,
            course_id,
            `order`
        ) VALUES (
                     #{dto.attractionId},
                     #{dto.travelId},
                     #{dto.courseId},
                     #{dto.order}
                 )
    </insert>

    <select id="findPlanById" resultType="PlanResponseDTO">
        SELECT
            id,
            attraction_id,
            travel_id,
            course_id,
            `order`
        FROM plans
        WHERE id = #{planId}
    </select>

    <select id="findPlansByUserId" resultType="PlanResponseDTO">
        SELECT
            p.id,
            p.attraction_id,
            p.travel_id,
            p.course_id,
            p.`order`
        FROM plans p
                 JOIN travels t ON p.travel_id = t.id
        WHERE t.user_id = #{userId}
        ORDER BY p.`order`
        LIMIT #{page.pageSize} OFFSET #{page.offset}
    </select>

    <select id="countPlansByUserId" resultType="int">
        SELECT COUNT(*)
        FROM plans p
                 JOIN travels t ON p.travel_id = t.id
        WHERE t.user_id = #{userId}
    </select>

    <update id="updatePlan">
        UPDATE plans
        SET
            attraction_id = #{dto.attractionId},
            course_id = #{dto.courseId},
            `order` = #{dto.order}
        WHERE id = #{planId}
    </update>

    <delete id="deletePlan">
        DELETE FROM plans
        WHERE id = #{planId}
    </delete>

</mapper>
